# 🤖 AI Agent 아키텍처 설계 (추천 순위별)

## 📌 전제 조건
- ✅ **데이터**: `studentId` (예: "586694_481") + `grade`만 보유
- ✅ **보안**: 개인 식별 정보 없음 → 보안 리스크 최소화
- ✅ **목표**: AI Agent로 발돋움하기 위한 아키텍처 구성

---

## 🏗️ 추천 아키텍처 (우선순위별)

### 🔴 **PRIORITY 1: 핵심 인프라 (즉시 구현, 1-2주)**

#### 1.1 학습 이력 수집 시스템
```
목적: 모든 학습 활동을 체계적으로 기록

아키텍처:
┌─────────────────┐
│  Frontend       │
│  (학습 활동)     │
└────────┬────────┘
         │ 이벤트 전송
         ▼
┌─────────────────┐      ┌──────────────────┐
│  Event Collector│ ───▶ │  Learning Events │
│  API            │      │  Collection      │
└─────────────────┘      └──────────────────┘
         │                       │
         │                       ▼
         │              ┌──────────────────┐
         └─────────────▶│  Agent Memory    │
                        │  Processor       │
                        └──────────────────┘
```

**구현 위치:**
- `app/api/events/route.ts` (이미 존재) → 강화
- `app/api/agent/memory/route.ts` (신규) → 메모리 처리

**수집 데이터:**
```typescript
interface LearningEvent {
  studentId: string; // "586694_481"
  grade: string; // "고2"
  eventType: 
    | 'problem_solved' // 문제 해결
    | 'problem_failed' // 문제 실패
    | 'concept_learned' // 개념 학습
    | 'quiz_completed' // 퀴즈 완료
    | 'session_start' // 세션 시작
    | 'session_end'; // 세션 종료
  metadata: {
    subject: string; // "수학"
    topic: string; // "이차방정식"
    difficulty: number; // 1-5
    timeSpent: number; // 초
    score?: number; // 점수 (0-100)
    mistakeType?: string; // 실수 유형
  };
  timestamp: Date;
}
```

**필요한 작업:**
1. ✅ `learning_events` 컬렉션 구조 확장
2. ✅ 이벤트 수집 API 강화
3. ⬜ 배치 처리 스케줄러 (일일 집계)

---

#### 1.2 Agent Memory 처리 시스템
```
목적: 학습 이벤트를 분석하여 agentMemory 업데이트

아키텍처:
┌──────────────────┐
│ Learning Events  │
│   (원시 데이터)   │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Memory Processor │ ──▶ 분석 및 집계
│   (배치 작업)     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Agent Memory    │
│  (students.col)  │
└──────────────────┘
```

**구현 파일:**
- `lib/agent-memory-processor.ts` (신규)

**처리 로직:**
```typescript
// 1. 학습 이벤트 집계
- 최근 주제 추출 (last 10 sessions)
- 자주 틀리는 유형 계산 (frequency > 3)
- 강점 영역 계산 (정답률 > 80%)
- 평균 점수 계산
- 학습 시간 패턴 분석

// 2. agentMemory 업데이트
- recentTopics: 최근 학습 주제 (상위 10개)
- frequentMistakes: 자주 틀리는 유형 (상위 20개)
- strengths: 강점 영역 (정답률 높은 주제)
- totalSessions: 총 세션 수
- averageScore: 평균 점수
```

**배치 처리:**
- 일일 1회 (새벽 2시)
- 또는 실시간 (이벤트 발생 시 즉시)

---

### 🟠 **PRIORITY 2: 분석 엔진 (2-3주)**

#### 2.1 학습 패턴 분석 엔진
```
목적: 학생의 학습 패턴을 분석하여 인사이트 도출

아키텍처:
┌──────────────────┐
│  Agent Memory    │
│   (현재 상태)     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Pattern Analyzer │
│   (분석 엔진)     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Learning Insights│
│   (인사이트)      │
└──────────────────┘
```

**구현 파일:**
- `lib/agent-pattern-analyzer.ts` (신규)

**분석 항목:**
```typescript
interface LearningPattern {
  studentId: string;
  grade: string;
  
  // 1. 성과 분석
  performance: {
    subject: string;
    trend: 'improving' | 'stable' | 'declining';
    currentLevel: number; // 1-5
    masteryScore: number; // 0-1
  }[];
  
  // 2. 실수 패턴
  mistakePattern: {
    type: string; // "계산 실수", "개념 혼동"
    frequency: number;
    contexts: string[]; // 발생 컨텍스트
    relatedTopics: string[]; // 관련 주제
  }[];
  
  // 3. 학습 효율성
  efficiency: {
    optimalTime: { hour: number; score: number }[]; // 집중도 높은 시간
    averageSessionLength: number; // 평균 세션 길이
    attentionSpan: number; // 집중 시간
  };
  
  // 4. 추천 사항
  recommendations: {
    priority: 'high' | 'medium' | 'low';
    type: 'review' | 'practice' | 'new_concept';
    topic: string;
    reason: string;
  }[];
}
```

**활용:**
- AI Agent가 학습 패턴을 이해 → 개인화된 설명
- 약점 예측 → 조기 개입
- 학습 계획 최적화

---

#### 2.2 적응형 학습 엔진
```
목적: 학생 수준에 맞는 난이도 및 학습 경로 조절

아키텍처:
┌──────────────────┐
│ Learning Pattern │
│   (분석 결과)     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Adaptive Engine  │
│  (난이도 조절)    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Learning Path    │
│  (최적 경로)      │
└──────────────────┘
```

**구현 파일:**
- `lib/agent-adaptive-engine.ts` (신규)

**기능:**
```typescript
interface AdaptivePlan {
  studentId: string;
  currentTopic: string;
  
  // 1. 난이도 조절
  difficulty: {
    recommended: number; // 1-5
    reason: string; // "최근 성적이 좋아서"
    nextStep: 'increase' | 'maintain' | 'decrease';
  };
  
  // 2. 학습 경로
  learningPath: {
    current: string;
    prerequisites: string[]; // 선수 개념
    next: string; // 다음 추천 주제
    alternative?: string; // 대안 경로
  };
  
  // 3. 학습 전략
  strategy: {
    approach: 'visual' | 'practice' | 'explanation';
    pace: 'slow' | 'normal' | 'fast';
    focus: string[]; // 집중할 부분
  };
}
```

**활용:**
- 문제 난이도 자동 조절
- 학습 순서 최적화
- 개인 맞춤 설명 방식

---

### 🟡 **PRIORITY 3: 예측 시스템 (3-4주)**

#### 3.1 성과 예측 모델
```
목적: 다음 퀴즈/시험 성과 예측 및 약점 조기 감지

아키텍처:
┌──────────────────┐
│ Learning Pattern │
│  + History       │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Prediction Model │
│   (AI 모델)      │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Predictions      │
│  (예측 결과)      │
└──────────────────┘
```

**구현 파일:**
- `lib/agent-predictor.ts` (신규)
- `app/api/agent/predict/route.ts` (신규)

**예측 항목:**
```typescript
interface Prediction {
  studentId: string;
  
  // 1. 다음 퀴즈 예측
  nextQuiz: {
    topic: string;
    predictedScore: number; // 0-100
    confidence: number; // 0-1
    riskFactors: string[]; // 위험 요소
  };
  
  // 2. 약점 조기 감지
  weakPointAlert: {
    topic: string;
    riskLevel: 'high' | 'medium' | 'low';
    trend: 'worsening' | 'stable';
    recommendation: string;
  }[];
  
  // 3. 숙달 예측
  masteryForecast: {
    topic: string;
    currentMastery: number; // 0-1
    estimatedDays: number; // 숙달까지 예상 일수
    requiredPractice: number; // 필요한 연습 문제 수
  }[];
}
```

**모델 선택:**
- 간단한 통계 모델 (평균, 추세) → 우선 구현
- 머신러닝 모델 (선형 회귀, 랜덤 포레스트) → 후속 단계

---

#### 3.2 조기 개입 시스템
```
목적: 문제가 커지기 전 미리 개입

아키텍처:
┌──────────────────┐
│ Predictions      │
│  (예측 결과)      │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Alert System     │
│  (경고 시스템)    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Intervention     │
│  (개입 액션)      │
└──────────────────┘
```

**구현 파일:**
- `lib/agent-intervention.ts` (신규)

**개입 시나리오:**
```typescript
interface Intervention {
  studentId: string;
  alertLevel: 'high' | 'medium' | 'low';
  reason: string; // "이차방정식 실수 빈도 증가"
  
  action: {
    type: 'review' | 'practice' | 'explain';
    topic: string;
    priority: number; // 1-10
    message: string; // AI Agent 메시지
  };
  
  timing: 'immediate' | 'next_session' | 'scheduled';
}
```

**활용:**
- AI Agent가 자동으로 약점 복습 제안
- 문제 커지기 전 미리 개입
- 학습 효율성 향상

---

### 🟢 **PRIORITY 4: 고급 기능 (4주+)**

#### 4.1 지식 그래프 시스템
```
목적: 개념 간 연관성 파악 및 학습 순서 최적화

아키텍처:
┌──────────────────┐
│ Concept Library  │
│  (개념 데이터)    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Knowledge Graph  │
│  (관계 그래프)    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Learning Order   │
│  (최적 순서)      │
└──────────────────┘
```

**구현 파일:**
- `lib/agent-knowledge-graph.ts` (신규)
- `data/concepts/` (데이터 파일)

**구조:**
```typescript
interface KnowledgeGraph {
  concepts: Map<string, ConceptNode>;
  relationships: Array<{
    from: string; // 개념 A
    to: string; // 개념 B
    type: 'prerequisite' | 'related' | 'similar';
    strength: number; // 0-1
  }>;
}

interface ConceptNode {
  id: string; // "quadratic_equation"
  name: string; // "이차방정식"
  subject: string; // "수학"
  grade: string[]; // ["중3", "고1"]
  prerequisites: string[]; // 선수 개념
  difficulty: number; // 1-5
}
```

**활용:**
- "이 개념을 이해하려면 먼저 A를 알아야 해"
- 학습 순서 자동 추천
- 개념 간 연결 설명

---

#### 4.2 멀티 에이전트 시스템
```
목적: 과목별 전문 에이전트로 분리

아키텍처:
┌──────────────────┐
│  Agent Router    │
│  (라우터)        │
└────────┬─────────┘
         │
    ┌────┴────┬─────────┬─────────┐
    ▼         ▼         ▼         ▼
┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│ Math   │ │English │ │Korean  │ │Science │
│ Agent  │ │ Agent  │ │ Agent  │ │ Agent  │
└────────┘ └────────┘ └────────┘ └────────┘
```

**구현 파일:**
- `lib/agents/math-agent.ts`
- `lib/agents/english-agent.ts`
- `lib/agents/korean-agent.ts`
- `lib/agents/base-agent.ts` (기본 클래스)

**활용:**
- 과목별 특화된 설명 방식
- 전문 도구 사용 (수학 계산기, 영어 문법 검사 등)
- 과목 간 협업 ("영어 문법과 한국어 문법 비교")

---

## 📁 파일 구조 제안

```
lib/
├── agent/
│   ├── memory/
│   │   ├── processor.ts          # 메모리 처리 (Priority 1)
│   │   └── updater.ts            # 메모리 업데이트
│   ├── analyzer/
│   │   ├── pattern-analyzer.ts   # 패턴 분석 (Priority 2)
│   │   └── performance-analyzer.ts
│   ├── adaptive/
│   │   ├── engine.ts             # 적응형 엔진 (Priority 2)
│   │   └── path-optimizer.ts
│   ├── prediction/
│   │   ├── predictor.ts          # 예측 모델 (Priority 3)
│   │   └── intervention.ts       # 조기 개입 (Priority 3)
│   ├── knowledge/
│   │   ├── graph.ts              # 지식 그래프 (Priority 4)
│   │   └── concepts.ts
│   └── agents/
│       ├── base-agent.ts         # 기본 에이전트 (Priority 4)
│       ├── math-agent.ts
│       └── english-agent.ts

app/api/
├── agent/
│   ├── memory/
│   │   └── route.ts              # 메모리 처리 API
│   ├── analyze/
│   │   └── route.ts              # 패턴 분석 API
│   ├── predict/
│   │   └── route.ts              # 예측 API
│   └── intervention/
│       └── route.ts              # 개입 API

data/
└── concepts/
    ├── math.json                 # 수학 개념 그래프
    ├── english.json              # 영어 개념 그래프
    └── korean.json               # 국어 개념 그래프
```

---

## 🔄 데이터 플로우

```
1. 학습 활동
   ↓
2. Event Collector (이벤트 수집)
   ↓
3. Learning Events Collection (원시 데이터 저장)
   ↓
4. Memory Processor (배치 처리, 일일 1회)
   ↓
5. Agent Memory 업데이트 (students.agentMemory)
   ↓
6. Pattern Analyzer (패턴 분석)
   ↓
7. Adaptive Engine (적응형 학습)
   ↓
8. Prediction Model (예측)
   ↓
9. Intervention System (조기 개입)
   ↓
10. AI Agent 응답 (개인화된 설명)
```

---

## ⚡ 구현 순서 요약

### Week 1-2: Priority 1 (핵심 인프라)
1. ✅ 학습 이벤트 수집 강화
2. ⬜ Agent Memory Processor 구현
3. ⬜ 배치 처리 스케줄러 (일일 집계)

### Week 3-4: Priority 2 (분석 엔진)
1. ⬜ Pattern Analyzer 구현
2. ⬜ Adaptive Engine 구현
3. ⬜ AI Agent 통합 (패턴 기반 설명)

### Week 5-6: Priority 3 (예측 시스템)
1. ⬜ Prediction Model 구현 (간단한 통계 모델)
2. ⬜ Intervention System 구현
3. ⬜ 조기 개입 통합

### Week 7+: Priority 4 (고급 기능)
1. ⬜ Knowledge Graph 구축
2. ⬜ Multi-Agent System 구현
3. ⬜ 고급 기능 통합

---

## 🎯 즉시 시작할 수 있는 것 (오늘!)

### 1. Event Collector 강화 (30분)
- `app/api/events/route.ts` 확장
- 더 많은 이벤트 타입 수집

### 2. Memory Processor 기본 구조 (2시간)
- `lib/agent/memory/processor.ts` 생성
- 간단한 집계 로직 구현

### 3. Pattern Analyzer 기본 (1시간)
- `lib/agent/analyzer/pattern-analyzer.ts` 생성
- 기본 패턴 분석 함수

---

## 💡 핵심 포인트

1. **점진적 확장**: 우선순위대로 하나씩 구현
2. **데이터 중심**: 모든 기능이 학습 이력 데이터 기반
3. **모듈화**: 각 기능을 독립적으로 개발 가능
4. **확장 가능**: 나중에 ML 모델 추가 가능한 구조
5. **보안 안전**: studentId + grade만 사용 (개인정보 없음)

---

## 📊 예상 효과 (단계별)

- **Priority 1 완료**: 기본 데이터 수집 및 분석 가능
- **Priority 2 완료**: 개인화된 학습 경로 제공
- **Priority 3 완료**: 예측 및 조기 개입 가능
- **Priority 4 완료**: 고급 AI Agent 시스템 완성

**최종 목표**: 각 학생에게 맞춤형 학습 경험 제공 🎯

